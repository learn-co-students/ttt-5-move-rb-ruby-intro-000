#!/usr/bin/env ruby

require_relative '../lib/move.rb'

# Code your CLI Here
board = Array.new(9, " ")
puts "Welcome to Tic Tac Toe!"
puts "Please enter the cell number of your next move."
input = input_to_index(gets.strip)
move(board, input)
display_board(board)
