#!/usr/bin/env ruby

require_relative '../lib/move.rb'

#Code your CLI Here
puts "Welcome to Tic Tac Toe!"
board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]
puts "Where would you like to go?"
input = gets.strip
#index[array] = input.to_i
#input = input.to_i
input_to_index(input)

move(board, 0, "X")
display_board(board)
